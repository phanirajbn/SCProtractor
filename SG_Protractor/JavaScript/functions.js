//Functions.js
function addFunc(first, second){
	return first + second;//Both a concatination and Addtion...
}

function subFunc(first, second){
	return first - second;
}

function mulFunc(first, second){
	return first * second;
}
function divFunc(first, second){
	return first / second;
}
